

void sendMessage() {    //skal også gerne sådan sørge for at den starter 
  if (fallConfirmed) {  //BT igen da denne bliver slukket når den går i power save
    //Send y
  }
  else {
    //send n
  }
}